[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short

markers =
    integration: marks tests as integration tests (requires running server)
    unit: marks tests as unit tests (no server required)
